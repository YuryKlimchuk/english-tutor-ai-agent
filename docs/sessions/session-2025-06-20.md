# Session 2025-06-20

## Что сделали

1. **Удалили внутренние `package-info.java`** в подпакетах (`api/`, `application/`, `domain/`, `infrastructure/`) всех 5 модулей — они были лишними, только корневые с `@ApplicationModule` имеют значение.

2. **Поправили скилл `spring-modulith-module`** (`/home/yury/.pi/agent/skills/spring-modulith-module/SKILL.md`) — убрали шаг создания `package-info.java` в подпакетах. Теперь скилл создаёт только пустые директории слоёв и корневой `package-info.java`.

## Текущее состояние проекта

```
english-tutor-ai-agent/
├── src/main/java/com/hydroyura/eta/
│   ├── EtaApplication.java
│   ├── teacher/         (api/ application/ domain/ infrastructure/)
│   ├── student/         (+)
│   ├── dictionary/      (+)
│   ├── lesson/          (+)
│   └── exercise/        (+)
├── pom.xml              (Spring Boot 4.1.0, Spring Modulith 1.1.10, Java 26)
└── docs/roadmap.MD      (план v1.0.0)
```

## Баги

- `EtaApplication.main` — package-private, нужно `public`

## Что дальше (по roadmap v1.0.0)

1. Добавить зависимости: Spring AI, PostgreSQL, MinIO, Telegram bot lib
2. Наполнять bounded context'ы реальным кодом (Teacher → Lesson → Dictionary → Student → Exercise)
3. Интеграция с Telegram bot
